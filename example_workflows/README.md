# Internal and compatibility examples

The supported end-user workflows are the three files in [`../workflows`](../workflows/).

Files in this directory are retained for API examples, compatibility tests, and
development history. They may expose experimental controls or older layouts and
are not the recommended starting point for a new installation. Bug reports
about first-run defaults should reproduce with a workflow from `workflows/`.
